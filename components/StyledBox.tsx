import { Container, styled, Box } from "@mui/system";

export const StyledBox = styled(Box)(() => ({
  justifyContent: "center",
  display: "flex",
  alignItems: "center",
  align: "left",
  backgroundRepeat: "no-repeat",
  backgroundSize: "cover",
}));
